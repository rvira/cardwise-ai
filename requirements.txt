# PDF ingestion
PyMuPDF==1.27.2.3

# Config / env loading
python-dotenv==1.2.2

# Chunking & embeddings (RAG pipeline)
langchain-core==1.4.7
langchain-text-splitters==1.1.2
langchain-experimental==0.4.2
langchain-google-genai==4.2.5

# Vector store
langchain-community==0.4.2
chromadb==1.5.9

# Frontend (demo)
streamlit==1.58.0

# Retrieval (experimental hybrid BM25 + semantic search)
rank-bm25==0.2.2

# Agent (LangGraph ReAct loop: tools + step cap + RAG fallback)
langgraph==1.2.9

# Evaluation (RAGAS scored via Gemini's OpenAI-compatible endpoint)
ragas==0.4.3
openai==2.30.0
