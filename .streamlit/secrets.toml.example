# CardWise secrets — TEMPLATE ONLY. Do NOT put the real key here.
#
# Local dev:   copy to `.streamlit/secrets.toml` (gitignored) OR use a `.env`.
# Cloud deploy: paste these keys/values into
#               Streamlit Community Cloud → your app → Settings → Secrets.
#
# Get a key at https://aistudio.google.com/apikey

GOOGLE_API_KEY = "your-gemini-api-key-here"
